pipeline {
    agent {
        docker { 
            image 'node:20-alpine'
            label 'docker'  // Directs execution to your dedicated docker-agent
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
